# NPM Configuration for TORP Platform
# This ensures compatibility with Vercel and handles peer dependencies

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Enable strict SSL
strict-ssl=true

# Save exact versions
save-exact=false
save-prefix=^

# Loglevel
loglevel=error

# Engine strict (enforce Node version)
engine-strict=false
